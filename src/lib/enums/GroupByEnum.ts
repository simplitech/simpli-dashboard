/**
 *  The application will show/group items according
 *  to the order specified by the following enum.
 */
export enum GroupByEnum {
  PROJECT = 'Project',
  ASSIGNEE = 'Assignee',
  STATUS = 'Status',
  DATE = 'Date',
}
